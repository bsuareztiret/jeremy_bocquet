services:
  nextjs:
    image: slaar/jijiwebsite:${TAG}

    labels:
      io.levv.public: "80/http"

    ports:
      - "80:3000"

    domainname: "www.jeremybocquet.xyz"

    environment:
      NODE_ENV: production
